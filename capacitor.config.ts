import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_Tres_Rayas;',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
